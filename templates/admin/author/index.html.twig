<div id="containerAdmin" class="containerAuthor">
    <h1>Auteur</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Nickname</th>
                <th>Created_date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for author in authors %}
            <tr>
                <td>{{ author.id }}</td>
                <td>{{ author.firstname }}</td>
                <td>{{ author.lastname }}</td>
                <td>{{ author.nickname }}</td>
                <td>{{ author.createdDate ? author.createdDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a class="btn btn-primary text-light" href="{{ path('author_show', {'id': author.id}) }}">Détail</a>
                    <a class="btn btn-primary text-light" href="{{ path('author_edit', {'id': author.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('author_new') }}">Create new</a>
</div>
