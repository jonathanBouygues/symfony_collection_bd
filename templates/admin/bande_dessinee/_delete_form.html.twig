<form method="post" action="{{ path('bande_dessinee_delete', {'id': bande_dessinee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bande_dessinee.id) }}">
    <button class="btn btn-primary m-2"> Supprimer</button>
</form>
