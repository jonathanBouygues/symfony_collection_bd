{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.purchase_date, { 'label': 'Date d\'achat' }) }}
    {{ form_row(form.price, { 'label': 'Prix' }) }}
    {{ form_row(form.bandeDessinee, { 'label': 'Bande Dessinée' }) }}

{{ form_end(form) }}
